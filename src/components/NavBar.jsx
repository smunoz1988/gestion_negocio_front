import React from 'react'
import Logout from './auth/Logout'

const Navbar = () => {
    return (
        <div>
            <p>navbar</p>
            <Logout />
        </div>
    )
};

export default Navbar